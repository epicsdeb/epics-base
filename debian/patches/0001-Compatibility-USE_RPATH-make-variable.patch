From: Michael Davidsaver <mdavidsaver@bnl.gov>
Date: Wed, 19 Sep 2012 10:46:41 -0400
Subject: Compatibility USE_RPATH make variable

Our legacy name for LINKER_USE_RPATH
---
 configure/CONFIG_SITE | 7 ++++++-
 1 file changed, 6 insertions(+), 1 deletion(-)

diff --git a/configure/CONFIG_SITE b/configure/CONFIG_SITE
index a37d48a..fe644cf 100644
--- a/configure/CONFIG_SITE
+++ b/configure/CONFIG_SITE
@@ -126,6 +126,11 @@ CROSS_COMPILER_HOST_ARCHS=
 #
 SHARED_LIBRARIES=YES
 
+#  Include RPATH when linking executables and libraries.
+#        must be either YES or NO
+#     Warning: Leave this set to YES unless you know what you are doing.
+USE_RPATH=YES
+
 #  Build client objects statically ?
 #        must be either YES or NO
 #
@@ -188,5 +193,5 @@ GCC_PIPE = NO
 #  Must be either YES or NO.  If you set this to NO you must also provide a
 #  way for Base executables to find their shared libraries when they are
 #  run at build-time, e.g. set the LD_LIBRARY_PATH environment variable.
-LINKER_USE_RPATH = YES
+LINKER_USE_RPATH = $(USE_RPATH)
 
